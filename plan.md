# genexcelbook

**NOTICE**
このファイルは指示プロンプトを作成するときの作業スペースです。
検討内容やテスト的に記述してある内容もあるので、私からの指示への対応時・コード生成時・仕様策定時に参考・参照したり、このファイルの内容に影響を受けてはいけません。

PowerShellスクリプトで以下の仕様に適合する物を作成してください。

## 仕様

- ImportExcelモジュールをインストール済みです。 importして使えるところは利用する。
- Excel非依存でPowerShellのコードによりExcel Bookを生成する。
- 生成するExcel Bookは ``.\vns-template.xlsx`` に示す。
- Book内のセルの内容が提示したExcel Bookと同一であること。
- Book内の書式設定などが提示したExcel Bookと同一であること。
- 以下の設定で合わせることができない機能がある場合は早めに教えてください。
  - 条件付き書式
  - 罫線・罫線の種類・罫線の太さ
  - 文字描画・フォント・文字色・背景色・ボールド等
  - セルデザイン・セル幅・セル高
  - フィルタの設定

## 手順について
以下の各ステップが完了するごとに、生成したコードと簡単な実行結果を提示してください。次のステップには進まず、私からの「続けてください」や「OKです」といった続行の指示があるまで待機してください。

1.  **Excel Bookの作成**:
    Excel Bookを作成し、空のシートを追加し、シート名を正しく設定するコードを生成して実行してください。
2.  **データと基本書式の設定**:
    ステップ1の状態から、シートにサンプルで指定されたデータを設定し、各列の幅、各行の高さ、セルのフォントとサイズ、フォントカラーやボールドなどの設定を行うコードを生成して実行してください。
3.  **罫線の適用**:
    ステップ2の状態から、罫線・罫線の種類・罫線の太さを適用するコードを生成して実行してください。
4.  **条件付き書式の適用**:
    ステップ3の状態から、条件付き書式を適用するコードを生成して実行してください。
5.  **フィルターの設定**:
    ステップ4の状態から、フィルターを設定するコードを生成して実行してください。

## ヒアリング

- **GEMINI.md**の指示に従って、適切にヒアリングを行うこと。
- 私の依頼の直後に主要の不明点などをヒアリングし、不明な項目がなくなるまでヒアリングを繰り返してください。
- ヒアリングの結果、不明な項目がゼロになってからコードの生成を開始します。
- 生成の各段階においてもテスト結果や生成時の使用を確認して修正依頼が発生することもあるのでその時点でも不明点が解消されるまでヒアリングを行ないます。

## 修正1

修正点がいくつか存在します。
以下の指摘をBuild-ToDo.mdにToDoとして記載し、順次生成・実行して結果を提示してください。

- 各セル内のデータ、文字列の内容、特に列名のセルは必ず内容を一致させるようにしてください。
- Sheet1は不要です、デフォルトで作成されてしまうのであれば削除するようにしてください。
- こちらの準備ミスですが、noteシートは作成する必要はないので、該当するコードは削除してください
- vns確認シートについて
  - 一行目に空行が入っており、一行目の高さ設定が無駄になっています。
  - どのようにずれているのかわかりませんがフィルターを設定する行も2行目になっています。
  - ヘッダを含めて29行、(罫線描画されている)表全体では30行必要なのに10行しか実装されていません。
  - 項目数が一三必要なところ7個しかありません
  - 項目名が適合していません、PowerShellで扱いにくい項目名であることは判っているので、CSVインポート後、書き換えを行う形で対応してください。
  - 条件付き書式も1つしか入っていません、正しくは3つで条件も一致していません、作りなおしてください。
