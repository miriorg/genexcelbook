# Build-ToDo.md

* 2026-01-10 06:44:36 ヒアリング項目追加
* **依頼内容**
  * `generate_excel.ps1` をベースに、外部CSVファイルを読み込んでExcel帳票を作成する `genXL.ps1` を作成する。
  * データ変換ルール（CurrentVersion/UpdateVersionの結合など）を実装する。
  * 凡例を出力する。
* **依頼日時**
  * 2026-01-10
* **現在依頼に対応している状況**
  * ステップ4完了（凡例追加実装済み・全工程完了）
* **決定事項**
  * なし
* **制限事項**
  * なし

## ヒアリング項目

* [x] **1. `plan.md` 記載の「修正1」の取り扱いについて**
  * 今回作成する `genXL.ps1` には、`plan.md` の「修正1」にある指摘事項（列名の修正、不要シートの削除、レイアウトの不備修正、条件付き書式の修正など）をすべて反映させる前提でよろしいでしょうか？
    * `plan.md`の内容はすべて無視すること。
    * 作成するExcelブックはすべて`generate_excel.ps1`の内容を引き継ぐこと（静的データを除く）
* [x] **2. 入力CSVファイルの仕様について**
  * スクリプトの引数名は `-CsvPath` でよろしいでしょうか？
    * YES
    * 未指定の場合、標準入力から受け取る
  * 想定されるCSVファイルの文字コードはありますか？（指定がない場合、UTF-8として実装します）
    * UTF-8
* [x] **3. 実行ステップの確認**
  * `GEMINI.md` および `prompt.md` の指示に従い、一気に完成品を作るのではなく、以下のステップごとにコードを提示し、確認を待ちます。この進め方でよろしいでしょうか？
    1. 引数処理とCSV読み込み・データ変換ロジックの実装
    2. Excelブック作成とシート設定（不要シート削除含む）
    3. ヘッダーとデータの書き込み
    4. 書式設定（列幅、フォント、罫線）
    5. 条件付き書式・フィルター・凡例の追加

## 実行計画

以下のステップ順に実装を進めます。各ステップ完了ごとにコードを提示し、確認を待ちます。

1.  **データ読み込みと変換処理の実装**
    *   引数 `-CsvPath` およびパイプライン入力の処理。
    *   CSVデータの読み込み（UTF-8）。
    *   指定されたルールに基づく `[現行バージョン／更新版バージョン]` 列の生成ロジック実装。
    *   **修正**: CSV列名 `[CurrentVersion]` への対応とデバッグ出力の強化。
    *   **修正**: `begin/process/end` ブロック構造の適正化。
    *   **修正**: 不要列（CurrentVersion, UpdateVersion, Note）の削除処理追加。
    *   **修正**: StrictMode下でのプロパティアクセスエラー修正。
    *   **修正**: `LoadFromCollection` の引数不足エラー修正。
    *   **修正**: `LoadFromCollection` を使用せず、ループ処理による書き込みに変更（安定性向上）。
    *   **修正**: PowerShellオブジェクトでの削除を諦め、Excel出力後に `DeleteColumn` で列削除を行う方式に変更。
    *   変換後のデータを `PSCustomObject` の配列として準備するところまで。
    *   **修正**: `Package` 列が `'-Not affected-'` の場合、`Package` 列を空文字、処理結果列を `'影響なし'` に置換する処理を追加。

## テスト実行手順（ユーザー実施用）

1. **単体実行（引数なし）**: `.\genXL.ps1` -> "CSV Path is not specified." の警告が出ればOK。
2. **CSV指定実行**: `.\genXL.ps1 -CsvPath "C:\Users\messe\git\vns\output\01070940.csv"` -> データのプレビューが表示されればOK。
2.  **Excelブック作成とデータ出力** (完了)
    *   `EPPlus` のロード、Excelパッケージ作成、ワークシート追加。
    *   ヘッダー行と変換済みデータの書き込み。
3.  **書式設定と条件付き書式** (完了)
    *   列幅、行の高さ、フォント、罫線、条件付き書式の適用。
4.  **凡例の追加と保存** (完了)
    *   凡例の描画とファイルの保存。
