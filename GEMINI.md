# 重要な注意事項

私の環境のgitフォルダーは 以下のような場所に存在します

```cmd
c:\users\<ユーザー名>\git
```

指示ファイルの中で 指定されたファイルのパスは、コンテキストのパスの中から実行時のユーザー名を識別し、指示ファイルは修正しようとせず、随時読み替えてください。
指示ファイル内のデフォルトのユーザー名は**miri**です、現環境との差分から修正を提案しようとせず、ユーザー名を読み替えるようにします。

プロジェクトごとにプロジェクトルートに Build-ToDo.md を作成します。
このファイルには、このプロジェクトに関する内容を随時・逐一記録します

* 私からの指示
* 指示を達成するための
  * 実行計画・段取り
  * 不足している情報
  * 指示では不明確な情報
* 私へのヒアリングすべき内容
* 私へのヒアリングの回答

## 慎重な実行姿勢

1. あなたが私から依頼を受けて作業を実行する場合、必ず ### 考慮事項 に挙げる点を考慮して実行してください。
2. ヒアリング項目が出力出来たらその旨を報告し、記入・回答を待ってください。 必要であればこれを繰り返します。
3. ヒアリング項目がない、ヒアリング項目がすべて解決した場合、成果物の作成に着手する前に成果物作成の段取りをToDoとしてプロジェクトルートの Build-ToDo.md に出力します。
4. 私に Build-ToDo.md の内容の了承を得てから、Build-ToDo.md の更新をしながら順次作業を進めてください。
5. 作業ステップが複数に渡る場合、または手順として定義されている場合は、一度にすべてのコードを生成せず、各ステップごとに実行可能な状態で停止し、私の確認と指示を待ってください。

### 考慮事項

* **ヒアリング項目について**
素早く回答を得られるよう、あなたが想定している内容などを選択肢として記載することを推奨します。
あなたの求めている内容・項目が含まれた文章を作成し、選択肢としてヒアリング項目に含めます。
* **依頼を受けた内容に問題点・不明点・あいまいな点はないか?**
内容が不明確な点や、依頼を実行するに当たって障害がある場合などは、 Build-ToDo.md にヒアリング項目として作成し、私にヒアリングを行ってください。
* **依頼を受けた内容に対して改善提案はないか?**
依頼内容に対して、セオリーやよくある事例を元に配慮が必要な事項の指摘など、改善に向けた提案があれば、 [提案を象徴するキーワード].ToDo.md というファイルを作成して提案内容を説明し、私に実施の可否ヒアリングを行ってください。

### ToDoファイルについて

* ヒアリング内容のやり取りを繰り返すにあたって、このファイルは追記のみとし、内容を削除してはいけません。
* 依頼事項からそれてしまわぬよう、ToDoファイルの先頭には必ず以下を記載してください
無理に一行にまとめず、箇条書きを活用すること。
* タスクやヒアリング項目が完了・解決した場合は、`[ ]` を `[x]` に変更するか、末尾に `(完了)` と記載してステータスを明確にしてください。
  * 追記するヒアリング項目の先頭に記載日時を記載する
  * **依頼内容**
  最初に依頼した内容から理解出来ている内容を整理して記述
  * **依頼日時**
  依頼を受けた日時
  * **現在依頼に対応している状況**
  最初のヒアリング・問題解決中など一言で状況が判る内容
  * **決定事項**
  依頼内容やヒアリングした結果確定した決定事項
  * **制限事項**
  依頼内容やヒアリングした結果確定した制限事項や制限範囲(対象範囲)

### 改善の提案

改善提案時には、出来るだけ以下の要素を含めるようにしてください。
無理に一行にまとめず、箇条書きを活用すること。

* 提案のタイトル
* 提案の背景
* 提案の内容
* 提案のメリットとデメリット
* 提案に必要なリソース
* 提案日時

## 一般的な項目

* 指定がない場合、日本語で応答する
* コード例を示す場合において、指定されていない場合はPowerShellを使う
* Microsoft 365関連の場合Office Scriptを使う（ただし、依頼内容で明示的にPowerShellや他ツールが指定されている場合を除く）
* 応答する文末には"(miri)"を付加すること

## ファイルシステムツール

- Edit_replaceを利用するときにはパラメータ名に「newContent:」ではなく「new_string:」を使うこと。

## コード生成について

* 関数の冒頭には最低でも「何をするための関数か記載」し、引数が多い(3個以上)時は引数の一覧と用途をコメントに追記すること。
* 返り値がある関数は必ず冒頭にコメントで記載する
* きちんと機能や処理内容など分割できる要素で分割して関数(function)化すること。
* 一つの関数に多くの機能を詰め込まないこと。
* 一つの関数が(目安として)50行を超えるような場合は、ヒアリング時に当該関数およびその上位の関数のリファクタリングを提案する。
* constな文字列は変数化すること
* PowerShellスクリプトを作成する場合、原則として `Set-StrictMode -Version Latest` を指定し、予期せぬ動作を防ぐこと。
* 外部モジュール（ImportExcelなど）を利用する場合は、スクリプト冒頭で `#Requires -Module モジュール名` を記述するか、存在確認を行うロジックを含めること。
* エラーハンドリング（Try-Catch）を適切に実装し、エラー発生時には詳細な情報を出力するようにすること。

## コード修正の依頼について

実行エラーのログを提示され、コードを修正する場合、以下の様に考えます。
3は1,2と同時には行わず、1回確認の往復をしてから3を実行してください。

1. typo等その行を書き換えれば済む内容の物を修正する
2. 「その行の前後」、「関数内」、「ファイル内の位置の修正」の修正する物を修正する
3. 「ロジックを変更する」、「広範囲な修正をする」ケースで、は一度広範囲な修正をする理由とその範囲を説明し、私に許可を得た後に、ToDoとして作業の一覧を作成してから広範囲の修正をする
4. 広範囲の修正を行う必要がある場合、現状のコミットの許可をとってからコミットしブランチを設定してから対応してください

## コード生成時の失敗の蓄積について

実行エラーや問題を報告されたときには、
その原因を特定し、エラーの内容・発生の原因・対応策を作成します。
私に提示して承認を取ってから code-generate-knowledge.md に記録します。
記録が完了したら準備した対応策を実行してください。

**記録フォーマット例:**

```markdown
## [YYYY-MM-DD] エラー概要
* **発生したエラー**: エラーメッセージやコード
* **原因**: 原因の詳細
* **対応策**: 修正内容や回避策
```

## code-generate-knowledge.md について

**c:\users\miri\git\code-generate-knowledge.md** はプロジェクトフォルダの外に配置します。
内容を削除してはいけません、追記のみ許可されます。
